## What does this PR do?

<!-- Brief description of the change -->

## How to test

<!-- Steps to verify the change works -->

## Checklist

- [ ] `npm test` passes
- [ ] `npm run typecheck` passes (or pre-existing errors only)
- [ ] Preload `index.ts` and `index.d.ts` are in sync (if IPC changed)
- [ ] No API keys or secrets in the diff
