node_modules/
out/
dist/
.node/
.env
# .claude/ (kept for project-local commands)
.claude/settings.local.json
.claude/cache/
.mcp.json
.playwright-mcp/

# macOS / Xcode / SwiftPM build artifacts
.DS_Store
**/xcuserdata/
*.xcuserstate
**/.build/
**/.swiftpm/
DerivedData/
# Xcode build output (scoped to mobile so it never touches desktop/build,
# which is tracked electron-builder buildResources)
mobile/**/build/
# Apex (per-machine state; signals/research/loops/tasks ARE tracked)
.apex/.lock
.apex/*.lock
.apex/dry-run-*.md
.apex/*.tmp

# Claude Code workflow worktrees
.claude/worktrees/
# test coverage artifacts (do not track)
desktop/coverage/
coverage/
DerivedData-Sim/
# Playwright click-test harness artifacts (P0.1) — reports/traces from local runs
desktop/test-results/
desktop/playwright-report/
desktop/blob-report/
mobile/WhisperioApp/DerivedData*/
